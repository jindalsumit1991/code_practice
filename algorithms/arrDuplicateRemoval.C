#include <iostream>
#include <vector>

int main()
{
    int size;
    cout << "Enter size of array: ";
    cin >> size;
    vector<int> v(size);

    for(int i=0; i<size; i++)
        cin >> v[i];

    for(int i=0; i<size; i++)
    {
        for(int j=0; j<size; j++)
        {

        }
    }

    return 0;
}
